# Configuration for deploying to Railway
[build]
  # The command to install dependencies
  install = "pip install -r requirements.txt"

[deploy]
  start = "STREAMLIT_SERVER_PORT=$PORT streamlit run sitemap_streamlit.py --server.port=$STREAMLIT_SERVER_PORT"

# Use Railway environment variables instead of .env file
